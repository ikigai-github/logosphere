import { GQLQueryType } from "./gql-query.type";

/**
 * Tepresents a GQL mutation definition
 * note: I haven't seen a difference in the schema structure, so just copying the query for now.
 */
export class GQLMutationType extends GQLQueryType {}
