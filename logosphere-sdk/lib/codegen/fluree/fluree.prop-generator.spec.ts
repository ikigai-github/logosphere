import { FlureePropGenerator } from './fluree.prop-generator';
describe('Fluree Schema Prop Generator', () => {
  const propGen = new FlureePropGenerator();
  it('should generate scalar', () => {});
});
