name: 'Build Example Music Moule'
on:
  workflow_dispatch:
  workflow_call:

permissions: {}
description: 'Build the example music module'
jobs:
  check-links:
    runs-on: ubuntu-latest
    steps:
      - name: Generate The Music Module
        run: echo "hello"
