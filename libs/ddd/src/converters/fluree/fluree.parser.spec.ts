import {
  select,
  compile,
  FlureeClient,
  FlureeCollection,
  FlureeService,
} from '@logosphere/fluree';
describe('Fluree schema parser', () => {
  it('should parse Fluree schema', async () => {
    // const fluree = new FlureeService();
    // const spec = compile(select().from('_collection').build());
    // console.log(JSON.stringify(spec, null, 2))
    // const collections  = await fluree.query('http://localhost:8090', 'local/cb', spec);
    // console.log(JSON.stringify(collections, null, 2));
    //expect(false).toBeTruthy()
  });
});
