{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Resolver",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the resolver"
    },
    "module": {
      "type": "string",
      "description": "Module for which resolver should be generated",
      "x-prompt": "What module is it for?",
      "alias": "m"
    },
    "directory": {
      "type": "string",
      "description": "A directory where the resolver is placed",
      "alias": "d"
    },
    "definition": {
      "type": "object",
      "description": "Canonical schema definition (single object)"
    },
    "index": {
      "type": "array",
      "description": "Canonical schema definitions to index"
    }
  },
  "required": ["module"]
}
